#include <stdio.h>
#include <string.h>
int main (void)
/**
* main - entry point
*
*Return: 0 after printing the function
*/
{
char str1[] = "with proper grammar, but the outcome is a piece of art,";
printf(str1);
return (0);
}
